commands
    nano
    help [command name]

basic file system
    mv
    rm
    cp

upload command will overwrite files without prompting
ensure file names are unique in a folder

"apps" in iframe
    access to core libraries via message passing

tab completion
ctrl+c to abort entry
save state to localstorage
